# Proxy pour l'API (redirige /api/... vers le backend en gardant l'URL du navigateur)
/api/* https://api.eidos-simul.fr/api/:splat  200

# Proxy pour l'Authentification
/auth/* https://api.eidos-simul.fr/auth/:splat  200

# Proxy pour Socket.io (Temps réel)
/socket.io/* https://api.eidos-simul.fr/socket.io/:splat 200